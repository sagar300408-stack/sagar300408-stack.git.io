import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/SUPABASE_URL="(.+)"/)[1]; const key = env.match(/SUPABASE_ANON_KEY="(.+)"/)[1]; const supabase = createClient(url, key); async function run() { const { data, error } = await supabase.from('organizations').select('id, name, slug, website, industry, company_size, phone, email, description, products_services, target_market, business_model, growth_stage, business_goals, address_line1, address_line2, city, state, postal_code, country').limit(1); console.log('Error:', error); } run();